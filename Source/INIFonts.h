#pragma once

/**
 * @file INIFonts.h
 * @brief Font-related constants for OTTO
 * 
 * This header contains all font-related constants used throughout the OTTO application.
 * Split from INIConfig.h for better maintainability and faster compilation.
 * 
 * @author OTTO Development Team
 * @version 2.0
 * @date 2024
 */

namespace INIConfig {

   // ========================================================================
   // FONTS NAMESPACE
   // ========================================================================
   namespace Fonts {
       static const float BODY_DEFAULT_SIZE = 14.0f;
       static const float HEADER_DEFAULT_SIZE = 18.0f;
       static const float MAX_FONT_SIZE = 72.0f;
       static const float NUMERIC_DEFAULT_SIZE = 16.0f;
       static const float BRAND_DEFAULT_SIZE = 20.0f;
       static const float BUTTON_DEFAULT_SIZE = 14.0f;
       static const float ICON_DEFAULT_SIZE = 16.0f;
       static const float VERSION_DEFAULT_SIZE = 12.0f;
       static const float POPUP_DEFAULT_SIZE = 14.0f;
   } // namespace Fonts

} // namespace INIConfig